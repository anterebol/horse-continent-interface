import { FormEvent } from '../../components/Forms/FormEvent/FormEvent';

export const Events = () => {
  return (
    <div>
      <h2>Добавить мероприятие </h2>
      <FormEvent clsInput={[]} idFor={''} />
    </div>
  );
};
